{
  "name": "sber-contest-2024",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare & npm run mock-server:gen-large-db",
    "mock-server:gen-large-db": "node mock-server/gen-large-db.cjs",
    "mock-server:start": "json-server mock-server/db.json -s node_modules/bootstrap-icons/icons",
    "mock-server:start:large": "json-server mock-server/large-db.json -s node_modules/bootstrap-icons/icons"
  },
  "dependencies": {
    "@pinia/nuxt": "^0.5.5",
    "nuxt": "^3.13.0",
    "pinia": "^2.2.4",
    "vue": "3.5.12",
    "vue-router": "4.4.5"
  },
  "devDependencies": {
    "bootstrap-icons": "^1.11.3",
    "json-server": "^1.0.0-beta.3"
  }
}
